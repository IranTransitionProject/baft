# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/
dist/
build/

# Environment
.env
.env.*

# Telegram session credentials (Telethon MTProto session — treat like SSH key)
*.session
*.session-journal

# Workspace — regenerated from framework YAML + Telegram exports
itp-workspace/
*.duckdb
*.duckdb.wal

# Telegram exports — raw data, not committed
pipeline/exports/
*.json.export

# Test artifacts
.pytest_cache/
htmlcov/
.coverage

# Editor
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# MkDocs build output
site/

# Loom runtime
watcher.lock
watcher.log

# Worker runtime artifacts
.resolved-configs/
.worker-logs/
.worker-pids

# NI findings are committed (analytical record), workspace data is not
# pipeline/ni_findings/ is tracked
